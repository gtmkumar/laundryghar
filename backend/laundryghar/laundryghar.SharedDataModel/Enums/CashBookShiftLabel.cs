namespace laundryghar.SharedDataModel.Enums;

/// <summary>
/// Matches finance_royalty.cash_books.shift_label CHECK constraint exactly.
/// Values: morning, afternoon, evening, night, full_day.
/// </summary>
public enum CashBookShiftLabel
{
    Morning,
    Afternoon,
    Evening,
    Night,
    FullDay
}
