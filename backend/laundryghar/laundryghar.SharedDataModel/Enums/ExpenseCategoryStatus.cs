namespace laundryghar.SharedDataModel.Enums;

/// <summary>
/// Matches finance_royalty.expense_categories.status CHECK constraint exactly.
/// Values: active, inactive, archived.
/// </summary>
public enum ExpenseCategoryStatus
{
    Active,
    Inactive,
    Archived
}
