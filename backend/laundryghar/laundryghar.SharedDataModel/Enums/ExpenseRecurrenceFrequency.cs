namespace laundryghar.SharedDataModel.Enums;

/// <summary>
/// Matches finance_royalty.expenses.recurrence_frequency CHECK constraint exactly.
/// Values: weekly, monthly, quarterly, yearly.
/// </summary>
public enum ExpenseRecurrenceFrequency
{
    Weekly,
    Monthly,
    Quarterly,
    Yearly
}
