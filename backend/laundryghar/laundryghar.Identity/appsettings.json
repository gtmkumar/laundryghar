{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "Default": "Host=localhost;Port=5432;Database=laundry_ghar_db;Username=postgres;Password=postgres"
    // "AppRuntime": "Host=localhost;Port=5432;Database=laundry_ghar_db;Username=app_user;Password=app_user"
    // Flip to AppRuntime in Phase 3 to test RLS isolation with the non-superuser role.
    // Run db/patches/app_user_role.sql first to create the app_user role with schema grants.
  },
  "Jwt": {
    "Issuer": "laundryghar-identity",
    "Audience": "laundryghar-services",
    "AccessMinutes": 15,
    "RefreshDays": 30
  },
  "Otp": {
    "TtlMinutes": 5,
    "MaxAttempts": 3,
    "ResendCooldownSeconds": 60
  },
  "CustomerAuth": {
    // DefaultBrandCode is used when no X-Brand-Id header or brandCode body param is provided.
    // Set to the seeded brand's code. Override per deployment.
    "DefaultBrandCode": "LG-MAIN"
  },
  "HealthChecks": {
    "NpgsqlConnectionString": "Host=localhost;Port=5432;Database=laundry_ghar_db;Username=postgres;Password=postgres"
  }
}
